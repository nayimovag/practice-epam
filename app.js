class Calculator{
    add(first, second) {
        let result = first + second
        return result
    }
    multiply(first, second) {
        let result = first * second
        return result
    }
}
let example = new Calculator
module.exports = example;